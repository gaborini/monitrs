interface:
  display_name: "Monitrs"
  short_description: "Repo-specific patterns and workflows for monitrs"
  default_prompt: "Use the monitrs repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true